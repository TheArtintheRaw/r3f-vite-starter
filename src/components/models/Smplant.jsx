/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.13 smplant.glb -d -I -R 512
Files: smplant.glb [31.57KB] > smplant.glb [6.08KB] (81%)
*/

import React, { useRef, useMemo, useContext, createContext } from 'react'
import { useGLTF, Merged } from '@react-three/drei'

const context = createContext()
export function Instances({ children, ...props }) {
  const { nodes } = useGLTF('/smplant.glb')
  const instances = useMemo(
    () => ({
      Object: nodes.Object_2001,
      Object1: nodes.Object_2001_1,
    }),
    [nodes]
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances) => <context.Provider value={instances} children={children} />}
    </Merged>
  )
}

export function Model(props) {
  const instances = useContext(context)
  return (
    <group {...props} dispose={null}>
      <instances.Object />
      <instances.Object1 />
    </group>
  )
}

useGLTF.preload('/models/items/smplant.glb')
